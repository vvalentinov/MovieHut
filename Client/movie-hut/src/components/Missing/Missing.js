export const Missing = (props) => {
    return (
        <>
            <p className="text-center display-6">{props.message}</p>
            <div style={{ height: '100vh' }}>
            </div>
        </>
    )
}